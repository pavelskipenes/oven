name: General CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    - name: build and install
      run: make -j$(nproc); sudo make install; sudo make uninstall
